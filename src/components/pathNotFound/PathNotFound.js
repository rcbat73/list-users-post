import * as React from "react";
import MainWrapper from "./MainWrapper";

const PatNotFound = () => (
    <MainWrapper>
        <p>There is no content!</p>
    </MainWrapper>
);

export default PatNotFound;
