import styled from "styled-components";

const Picture = styled("img")`
  border-radius: 10px;
  width: 200px;
  border: 2px solid var(--white);
  box-shadow: 0 3px 8px var(--taupe-gray);
`;

export default Picture;
